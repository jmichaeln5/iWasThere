
<p id="notice"><%= notice %></p>

<h1>My Places</h1>

<p> You have <strong> <%= @places.count%> </strong> Places. </p>

<%#= form_tag("/search", method: "get") do %>
  <%#= label_tag(:q, "Search Places:") %>
  <%#= text_field_tag(:q) %>
  <%#= submit_tag("Search") %>
<%# end %>

<!-- <hr>
<%#=link_to 'Directions', directions_path(current_user) %>
<hr> -->

<br>
  <table class="center">
    <thead>
      <tr>
        <!-- <th>USER ID</th> -->
        <th>Photo</th>
        <th>Place</th>
        <th>Rating</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @places.order("created_at DESC").each do |user_place| %>
        <tr>
          <td><%= image_tag user_place.image.url(:thumb)%></td>
          <td><%= user_place.title %></td>
          <td><%= user_place.rating %></td>

          <td><%=link_to 'Show', user_place_path(@user, user_place) %></td>

        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

   <%= link_to ' Back', :back %>

   <br>
   <br>
